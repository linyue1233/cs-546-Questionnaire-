

<article class="new-question">
  <h1>Ask a Question</h1>
  <form action="/questions" name="new_question" method="POST" onsubmit="return validateCommunity()" class="new-question-form">
        <label >  Community: <br>
        <select class="full-width-input" name="community">
      <option value="">Select community...</option>
      {{#each this.com}}
        <option value="{{this._id}}">{{this.name}}</option>
      {{/each}}
      
    </select>
   
    </label>
    <br>
    <label>
       Title: <br>
      <input type="text" name="title" placeholder="Enter Title" value="{{title}}" class="full-width-input" required/>
    </label>
   <br>
    <label>
        Description: <br>
      <textarea id="Question-Description"  rows="6"  name="description" placeholder="Enter Description" class="full-width-input" required>{{description}}</textarea>

    </label>
  <br>
    <label>
       Tags: <br>
      <input type="text" name="tags" placeholder="Tags seprated by spaces" value="{{tags}}" class="full-width-input" required/>
    </label>
    <br>
    <br>
    </label> 
        <button type="submit" class="btn btn-secondary btn-sm">Submit</button>
     
    </label>
   

  
  </form>



{{#if hasErrors}}
  <ul class="error-list">
    {{#each errors}}
    <li>
      {{this}}
    </li>
    {{/each}}
  </ul>
  {{/if}}
</article>

